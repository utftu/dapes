{
  "name": "dapes",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.11",
  "main": "dist/dapes.js",
  "types": "dist/types/dapes.d.ts",
  "scripts": {
    "build1": "echo $RANDOM && echo hello",
    "build": "python3 -c 'from datetime import datetime; print(datetime.now().strftime(\"%Y-%m-%d %H:%M:%S.%f\"))' && bun build ./src/dapes.ts --target bun --outdir ./dist",
    "types": "tsc --project tsconfig.types.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "argblock": "0.0.9",
    "typescript": "^5"
  }
}